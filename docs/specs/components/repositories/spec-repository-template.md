# {{repository-name}} Repository

## Статус: {{статус}}

## 1. Описание

### 1.1 Назначение

{{Краткое описание назначения репозитория. Какие сущности он управляет и какие операции обеспечивает.}}

### 1.2 Границы ответственности

{{Описание границ ответственности репозитория. Что входит и что НЕ входит в ответственность.}}

| Входит | НЕ входит |
|--------|-----------|
| {{Operation 1}} | {{External concern 1}} |
| {{Operation 2}} | {{External concern 2}} |

### 1.3 Связанные домены и сущности

| Домен | Сущность | Роль |
|-------|----------|------|
| {{domain}} | {{entity}} | {{input/output/intermediary}} |

### 1.4 Связанные спецификации

| Спецификация | Тип | Ссылка |
|--------------|-----|--------|
| {{spec-name}} | {{spec-type}} | [Ссылка](../path/to/spec.md) |

---

## 2. Интерфейс репозитория

### 2.1 Типы данных

#### Create<{{Entity}}>Data

| Поле | Тип | Обязательное | Описание |
|------|-----|--------------|----------|
| {{field}} | {{type}} | да/нет | {{description}} |

#### Update<{{Entity}}>Data

| Поле | Тип | Обязательное | Описание |
|------|-----|--------------|----------|
| {{field}} | {{type}} | да/нет | {{description}} |

### 2.2 Методы интерфейса

#### create

Создаёт новую запись в базе данных.

| Параметр | Тип | Обязательное | Описание |
|----------|-----|--------------|----------|
| data | Create<{{Entity}}>Data | да | Данные для создания |
| tx? | TransactionClient | нет | Транзакционный контекст |

| Возвращает | Тип | Описание |
|------------|-----|----------|
| {{return-type}} | {{type}} | {{description}} |

| Выбрасывает | Когда | Код ошибки |
|-------------|--------|-------------|
| UniqueConstraintError | {{condition}} | {{error-code}} |
| RepositoryError | {{condition}} | {{error-code}} |

#### findById

Находит запись по первичному ключу.

| Параметр | Тип | Обязательное | Описание |
|----------|-----|--------------|----------|
| id | string | да | ID записи |

| Возвращает | Тип | Описание |
|------------|-----|----------|
| {{return-type}} | {{type} \| null} | {{description}} |

#### findAll

Находит записи с пагинацией и фильтрацией.

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| query | PaginationOptions | - | Параметры пагинации |
| filters? | FilterOptions | - | Параметры фильтрации |

| Возвращает | Тип | Описание |
|------------|-----|----------|
| result | PaginatedResult<{{Entity}}> | {{description}} |

#### update

Обновляет существующую запись.

| Параметр | Тип | Обязательное | Описание |
|----------|-----|--------------|----------|
| id | string | да | ID записи |
| data | Update<{{Entity}}>Data | да | Новые данные |
| tx? | TransactionClient | нет | Транзакционный контекст |

| Возвращает | Тип | Описание |
|------------|-----|----------|
| {{return-type}} | {{type}} | {{description}} |

#### delete

Удаляет запись по ID.

| Параметр | Тип | Обязательное | Описание |
|----------|-----|--------------|----------|
| id | string | да | ID записи |
| tx? | TransactionClient | нет | Транзакционный контекст |

| Возвращает | Тип | Описание |
|------------|-----|----------|
| void | void | {{description}} |

### 2.3 Расширенные методы (Relations)

Методы для работы со связанными сущностями.

#### findByIdWith<{{RelatedEntity}}>

Находит запись с предзагруженными связями.

| Параметр | Тип | Обязательное | Описание |
|----------|-----|--------------|----------|
| id | string | да | ID записи |

| Возвращает | Тип | Описание |
|------------|-----|----------|
| {{return-type}} | {{type} \| null} | {{description}} |

---

## 3. Реализация

### 3.1 Структура файлов

| Файл | Назначение |
|------|------------|
| `src/repositories/<{{entity}}>-repository.ts` | Интерфейс репозитория |
| `src/repositories/_impl/<{{entity}}>-repository-impl.ts` | Конкретная реализация |
| `src/repositories/_lib/errors.ts` | Классы ошибок |
| `src/repositories/_types/pagination.ts` | Типы пагинации |

### 3.2 ORM особенности

{{Описание особенностей реализации с используемой ORM (Prisma/TypeORM).}}

#### Маппинг типов

| ORM тип | TypeScript тип | Описание |
|---------|----------------|----------|
| {{orm-type}} | {{ts-type}} | {{description}} |

#### Транзакции

{{Описание поддержки транзакций.}}

| Метод | Поддержка | Описание |
|-------|-----------|----------|
| {{method}} | да/нет | {{description}} |

### 3.3 Обработка ошибок

#### Классы ошибок

| Ошибка | Когда выбрасывается | Код | HTTP статус |
|--------|---------------------|-----|-------------|
| RepositoryError | {{condition}} | {{code}} | {{status}} |
| UniqueConstraintError | {{condition}} | {{code}} | {{status}} |
| DatabaseConnectionError | {{condition}} | {{code}} | {{status}} |

#### Пример обработки

```typescript
try {
  return await orm.entity.create(data);
} catch (error) {
  if (error.code === 'P2002') {
    throw new UniqueConstraintError('uniqueField');
  }
  throw new RepositoryError(error.message);
}
```

---

## 4. Индексы и оптимизация

### 4.1 Индексы базы данных

| Поле | Тип | Назначение |
|------|-----|------------|
| {{field}} | {{index-type}} | {{description}} |

### 4.2 Оптимизация запросов

{{Описание стратегий оптимизации: выборка только нужных полей, предотвращение N+1, batch-операции.}}

| Оптимизация | Описание | Когда применять |
|-------------|----------|-----------------|
| {{optimization}} | {{description}} | {{condition}} |

---

## 5. Тестирование

### 5.1 Unit тесты

| Тест | Сценарий | Ожидаемый результат |
|------|----------|---------------------|
| {{test-name}} | {{scenario}} | {{expected}} |

### 5.2 Integration тесты

| Тест | Сценарий | Ожидаемый результат |
|------|----------|---------------------|
| {{test-name}} | {{scenario}} | {{expected}} |

### 5.3 No-Mock Policy

Тесты репозиториев должны использовать реальную БД в транзакциях с последующим откатом (Rollback).

---

## 6. Зависимости

### 6.1 Внутренние зависимости

| Зависимость | Тип | Назначение |
|-------------|-----|------------|
| {{dependency}} | {{type}} | {{purpose}} |

### 6.2 Внешние зависимости

| Пакет | Версия | Назначение | Согласовано |
|-------|--------|------------|-------------|
| {{package}} | {{version}} | {{purpose}} | {{yes/no}} |

---

## 7. Чек-лист качества

- [ ] Интерфейс репозитория определён и типизирован
- [ ] Все публичные методы имеют JSDoc аннотации
- [ ] Реализация скрыта от внешних сущностей (_impl/)
- [ ] Ошибки БД транслируются в доменные ошибки
- [ ] Поддержка транзакций через TransactionClient
- [ ] Нет возврата объектов ORM напрямую (только чистые объекты)
- [ ] Индексы добавлены для часто запрашиваемых полей
- [ ] Integration тесты написаны с No-Mock Policy
- [ ] Сырые SQL запросы используют параметризированный ввод
- [ ] Factory функция для создания экземпляра репозитория

---

## 8. История изменений

| Версия | Дата | Изменения | Автор |
|--------|------|-----------|-------|
| 0.1.0 | {{YYYY-MM-DD}} | Начальная версия | {{автор}} |
