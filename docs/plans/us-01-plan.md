# План реализации: US-01 Автоматическое назначение роли GUEST при регистрации

> **Шаблон используется для декомпозиции User Story на технические задачи по слоям архитектуры.**  
> Шаблон создаётся на этапе PLAN (Спецификация) и передаётся в Code-режим для выполнения.  
> Статусы задач: `[TODO]` → `[IN PROGRESS]` → `[DONE]`

---

## 📋 Метаданные

| Параметр       | Значение                          |
| -------------- | --------------------------------- |
| **US-ID**      | `US-01`                           |
| **Название**   | Автоматическое назначение роли GUEST при регистрации |
| **Версия плана** | `v1.0`                            |
| **Дата создания** | `2026-07-09`                   |
| **Статус**     | `[TODO]`                          |
| **Зависит от** | Нет                               |

---

## 📎 Ссылки

- **User Story:** [`docs/user-stories/US-01-автоматическое-назначение-роли-GUEST-при-регистрации.md`](../user-stories/US-01-автоматическое-назначение-роли-GUEST-при-регистрации.md)
- **Требования (REQ):** `docs/requirements/REQ-ROLES-001.md` (FR-02, BR-01, BR-02, BR-06)
- **Модель данных:** `docs/model/entities/user.md`, `docs/model/entities/role.md`, `docs/model/entities/user-role.md`

---

## 📁 Дерево файлов

```
src/
├── domains/
│   ├── auth/
│   │   ├── auth.repository.prisma.ts          # ✏️ Изменить: выбрасывать ошибку при отсутствии GUEST
│   │   └── auth.errors.ts                      # ✏️ Изменить: добавить UserRegistrationError
│   └── roles/
│       ├── roles.errors.ts                     # ✏️ Изменить: добавить GuestRoleNotFoundError
│       └── index.ts                            # ✏️ Изменить: re-export новой ошибки
```

> **Примечание:** Новая модель данных, типы, валидаторы, репозитории, сервисы, UI или страницы **не создаются**. Изменения ограничены существующим кодом.

---

## 📦 Задачи по слоям

---

### 1. Модель данных (Prisma + DBML)

#### Задача 1: Проверить наличие роли GUEST в seed-данных

| Параметр   | Значение                                         |
| ---------- | ------------------------------------------------ |
| **ID**     | `US-01-T1`                                       |
| **Статус** | `[TODO]`                                         |
| **Файл**   | `prisma/seed.ts`                                 |
| **Действие** | Проверить                                     |

**Целевое состояние:**

- Роль GUEST существует в `SEED_ROLES` массиве (строка 98 seed.ts)
- Убедиться, что `isSystem: true` для роли GUEST

**Чек-лист:**

- [ ] Роль GUEST присутствует в `SEED_ROLES` с `isSystem: true`
- [ ] `name: 'GUEST'` используется корректно для поиска в `prisma.role.upsert`

---

### 2. Types (Доменные типы)

#### Задача 1: Проверить доменные типы

| Параметр   | Значение                                         |
| ---------- | ------------------------------------------------ |
| **ID**     | `US-01-T2`                                       |
| **Статус** | `[TODO]`                                         |
| **Файл**   | `src/domains/auth/auth.types.ts`                 |
| **Действие** | Проверить                                     |

**Целевое состояние:**

- Типы `UserData`, `CreateUserInput` корректно определяют структуру
- Нет изменений — существующие типы подходят

**Чек-лист:**

- [ ] Типы `UserData`, `CreateUserInput` существуют и корректны
- [ ] Нет изменений, необходимых для US-01

---

### 3. Validators (Zod-схемы)

#### Задача 1: Проверить Zod-схемы регистрации

| Параметр   | Значение                                           |
| ---------- | -------------------------------------------------- |
| **ID**     | `US-01-T3`                                         |
| **Статус** | `[TODO]`                                           |
| **Файл**   | `src/domains/auth/auth.validators.ts`              |
| **Действие** | Проверить                                       |

**Целевое состояние:**

- Схемы валидации данных регистрации не меняются
- Назначение роли GUEST происходит на уровне репозитория, без UI-взаимодействия

**Чек-лист:**

- [ ] Схемы `registerSchema` и `loginSchema` не требуют изменений
- [ ] Нет изменений, необходимых для US-01

---

### 4. Errors (Доменные ошибки)

#### Задача 1: Создать ошибку отсутствия роли GUEST

| Параметр   | Значение                                         |
| ---------- | ------------------------------------------------ |
| **ID**     | `US-01-T4-1`                                     |
| **Статус** | `[TODO]`                                         |
| **Файл**   | `src/domains/roles/roles.errors.ts`              |
| **Действие** | Создать                                        |

**Целевое состояние:**

- Создан класс `GuestRoleNotFoundError`, наследующийся от `NotFoundError`
- JSDoc-аннотация: `@error`, `@domain`, сообщение на русском языке
- HTTP-статус: 500 (системная ошибка — отсутствует предустановленная роль)

**Чек-лист:**

- [ ] Класс ошибки наследуется от `NotFoundError` (или `BusinessRuleError`)
- [ ] Класс снабжён JSDoc-аннотацией (`@error`, `@domain`)
- [ ] Сообщение об ошибке на русском языке: "Системная роль GUEST не найдена"
- [ ] HTTP-статус указан: 500 (Internal Error)

**Пример реализации:**

```typescript
/**
 * @class GuestRoleNotFoundError
 * @domain roles
 * @description Системная роль GUEST не найдена в БД — регистрация невозможна
 * @throws HTTP 500
 */
export class GuestRoleNotFoundError extends BusinessRuleError {
  readonly name = 'GuestRoleNotFoundError';
  constructor() {
    super('Системная роль GUEST не найдена. Обратитесь к администратору.');
  }
}
```

#### Задача 2: Добавить ошибку для регистрации при отсутствии GUEST в auth.errors.ts

| Параметр   | Значение                                         |
| ---------- | ------------------------------------------------ |
| **ID**     | `US-01-T4-2`                                     |
| **Статус** | `[TODO]`                                         |
| **Файл**   | `src/domains/auth/auth.errors.ts`                |
| **Действие** | Создать                                        |

**Целевое состояние:**

- Создан класс `GuestRoleMissingError`, наследующийся от `BusinessRuleError`
- Используется в `AuthRepository.create` при отсутствии роли GUEST

**Чек-лист:**

- [ ] Класс ошибки наследуется от `BusinessRuleError`
- [ ] Класс снабжён JSDoc-аннотацией (`@error`, `@domain`)
- [ ] Сообщение об ошибке на русском языке
- [ ] Импортирован в `src/domains/auth/auth.errors.ts`

**Пример реализации:**

```typescript
/**
 * @class GuestRoleMissingError
 * @domain auth
 * @description Системная роль GUEST отсутствует в БД — регистрация отклоняется
 * @throws HTTP 500
 */
export class GuestRoleMissingError extends BusinessRuleError {
  readonly name = 'GuestRoleMissingError';
  constructor() {
    super('Системная роль GUEST не найдена. Регистрация невозможна.');
  }
}
```

#### Задача 3: Обновить index.ts домена roles

| Параметр   | Значение                                         |
| ---------- | ------------------------------------------------ |
| **ID**     | `US-01-T4-3`                                     |
| **Статус** | `[TODO]`                                         |
| **Файл**   | `src/domains/roles/index.ts`                     |
| **Действие** | Изменить                                       |

**Целевое состояние:**

- `GuestRoleNotFoundError` добавлен в re-export из `roles.errors`

**Чек-лист:**

- [ ] `GuestRoleNotFoundError` добавлен в блок `export { ... }` в `index.ts`

---

### 5. Repository (Интерфейс + Реализация)

#### Задача 1: Обновить AuthRepository.create для явной ошибки при отсутствии GUEST

| Параметр   | Значение                                       |
| ---------- | ---------------------------------------------- |
| **ID**     | `US-01-T5`                                     |
| **Статус** | `[TODO]`                                       |
| **Файл**   | `src/domains/auth/auth.repository.prisma.ts`   |
| **Действие** | Изменить                                     |

**Целевое состояние:**

- В `AuthRepository.create` при отсутствии роли GUEST выбрасывается `GuestRoleMissingError`
- Транзакция откатывается целиком — пользователь не создаётся без роли
- Превращает Prisma ошибку P2002 в `UserDuplicateError` — без изменений
- Превращает любую другую ошибку (включая P2002 для user_roles) в корректную обработку

**Чек-лист:**

- [ ] При `guestRole === null` выбрасывается `GuestRoleMissingError`
- [ ] Ошибка выбрасывается ВНУТРИ транзакции (`$transaction`)
- [ ] При ошибке транзакция полностью откатывается
- [ ] Обработка Prisma P2002 для email сохранена
- [ ] JSDoc `@spec` обновлён: описана новая логика ошибки
- [ ] JSDoc `@throws` обновлён: добавлен `GuestRoleMissingError`

**Текущая проблема:**
В текущей реализации (строки 117-130 `auth.repository.prisma.ts`), если роль GUEST не найдена — пользователь создаётся без роли (молча). Это нарушает AC-5 US-01.

**Необходимое изменение:**

```typescript
// После line 121 (const guestRole = await tx.role.findUnique(...))
if (!guestRole) {
  throw new GuestRoleMissingError();
}
```

---

### 6. Service (Бизнес-логика)

#### Задача 1: Обновить JSDoc в AuthService.registerUser

| Параметр   | Значение                                  |
| ---------- | ----------------------------------------- |
| **ID**     | `US-01-T6`                                |
| **Статус** | `[TODO]`                                  |
| **Файл**   | `src/domains/auth/auth.service.ts`        |
| **Действие** | Изменить                               |

**Целевое состояние:**

- JSDoc `@throws` в методе `registerUser` обновлён: добавлен `GuestRoleMissingError`
- JSDoc `@spec` обновлён: описана новая логика обработки отсутствия роли GUEST
- Реализация метода не меняется — обработка ошибки происходит в репозитории

**Чек-лист:**

- [ ] JSDoc `@throws` обновлён: добавлен `@throws {GuestRoleMissingError} если роль GUEST отсутствует в БД`
- [ ] JSDoc `@spec` шаг 5 обновлён: описана обработка отсутствия роли GUEST
- [ ] Комментарий "роль GUEST назначается в репозитории" обновлён

---

### 7. API Route Handler

#### Задача 1: Проверить /api/v1/auth/register/route.ts

| Параметр   | Значение                                              |
| ---------- | ----------------------------------------------------- |
| **ID**     | `US-01-T7`                                            |
| **Статус** | `[TODO]`                                              |
| **Файл**   | `src/app/api/v1/auth/register/route.ts`               |
| **Действие** | Проверить                                           |

**Целевое состояние:**

- Handler уже перехватывает `BaseError` и возвращает JSON-ответ с соответствующим HTTP-статусом
- `GuestRoleMissingError` будет автоматически обработан как HTTP 500

**Чек-лист:**

- [ ] Handler обрабатывает `instanceof BaseError`
- [ ] HTTP-статус для `GuestRoleMissingError` будет 500 (по умолчанию)
- [ ] Нет изменений в коде handler — только проверка

---

### 8. UI-компоненты

#### Задача 1: Проверить компоненты регистрации

| Параметр   | Значение                                                     |
| ---------- | ------------------------------------------------------------ |
| **ID**     | `US-01-T8`                                                   |
| **Статус** | `[TODO]`                                                     |
| **Файл**   | `src/hooks/useRegister.ts`, компоненты страницы регистрации |
| **Действие** | Проверить                                                 |

**Целевое состояние:**

- Нет изменений — автоматическое назначение роли GUEST прозрачно для пользователя
- Обработка ошибки 500 уже реализована в клиенте

**Чек-лист:**

- [ ] Нет изменений UI, необходимых для US-01
- [ ] Ошибка "роль GUEST не найдена" отображается как системная ошибка 500

---

### 9. DI Container

#### Задача 1: Проверить DI-зависимости

| Параметр   | Значение                           |
| ---------- | ---------------------------------- |
| **ID**     | `US-01-T9`                          |
| **Статус** | `[TODO]`                           |
| **Файл**   | `src/di/container.ts`              |
| **Действие** | Проверить                        |

**Целевое состояние:**

- Нет изменений в DI-контейнере — ошибка `GuestRoleMissingError` создаётся внутри транзакции
- Зависимости `AuthService` → `AuthRepository` не меняются

**Чек-лист:**

- [ ] Нет изменений в DI-контейнере
- [ ] Фабрика `createAuthService()` не меняется

---

## 📊 Матрица соответствия AC → Задачи

| AC | Описание | Задача | Статус |
|----|----------|--------|--------|
| AC-1 | Успешное автоматическое назначение роли GUEST | T5, T6 | [TODO] |
| AC-2 | Атомарность транзакции (откат при ошибке) | T1, T5 | [TODO] |
| AC-3 | Идемпотентность (составной PK user_roles) | T5 | [TODO] |
| AC-4 | Регистрация без дублирования при повторной отправке email | T5 (существующая логика P2002) | [TODO] |
| AC-5 | Регистрация отклоняется при отсутствии роли GUEST | T4-1, T4-2, T4-3, T5 | [TODO] |

---

## 📊 Матрица AC → Задачи (расширенная)

| AC | Описание | Задачи | Приоритет |
|----|----------|--------|-----------|
| AC-1 | Успешное автоматическое назначение GUEST | T1, T5, T6 | Высокий |
| AC-2 | Атомарность транзакции: откат при ошибке | T1, T5 | Высокий |
| AC-3 | Идемпотентность: составной PK (user_id, role_id) | T5 | Средний |
| AC-4 | Обработка дублирования email (существующая логика) | T5 | Низкий |
| AC-5 | Обработка отсутствия GUEST: отклонение регистрации | T4-1, T4-2, T4-3, T5 | Высокий |

---

## ✅ Чек-лист валидации плана

### Код и типы
- [ ] Все изменённые файлы имеют актуальные JSDoc-аннотации
- [ ] `@throws` обновлён для методов, которые теперь выбрасывают новые ошибки
- [ ] Ошибки наследуются от базовых классов (`BusinessRuleError`)
- [ ] Сообщения об ошибках на русском языке

### Целостность
- [ ] `prisma/schema.prisma` синхронизирован с `docs/model/schema.dbml` (без изменений)
- [ ] Seed-данные содержат роль GUEST (без изменений)
- [ ] DI-контейнер не требует изменений

### Тестирование
- [ ] `npm run type-check` — без ошибок
- [ ] `npm run lint` — без ошибок
- [ ] Существовающие тесты `tests/unit/domains/auth/` не ломаются
- [ ] Новые тесты для AC-5: `tests/unit/domains/auth/auth.repository.test.ts`

### Функциональность
- [ ] AC-1 пройден: регистрация создаёт User + UserRole с GUEST
- [ ] AC-2 пройден: транзакция откатывается при ошибке
- [ ] AC-3 пройден: составной PK предотвращает дублирование
- [ ] AC-4 пройден: дублирование email возвращает ошибку
- [ ] AC-5 пройден: отсутствие GUEST возвращает ошибку 500

---

## 📝 История изменений

| Дата       | Версия | Автор     | Изменение                   |
| ---------- | ------ | --------- | --------------------------- |
| 2026-07-09 | v1.0   | Architect | Создание плана реализации   |

---

## 📎 Дополнительные ссылки

- [`SPECS.md`](../../.roo/rules/SPECS.md) — правила управления спецификациями
- [`MODEL.md`](../../.roo/rules/MODEL.md) — правила управления моделью данных
- [`ARCHITECTURE.md`](../../.roo/rules/ARCHITECTURE.md) — правила архитектуры
- [`PROJECT.md`](../../.roo/rules/PROJECT.md) — глобальные правила проекта

---

## 🔍 Анализ текущей реализации

### Текущее состояние

| Компонент | Статус | Комментарий |
|-----------|--------|-------------|
| Роль GUEST в seed.ts | ✅ | Предустановлена (строка 98) |
| Транзакция в AuthRepository.create | ✅ | Используется `prisma.$transaction` |
| Поиск роли GUEST | ✅ | `findUnique({ where: { name: 'GUEST' } })` |
| Создание UserRole | ✅ | `tx.userRole.create()` внутри транзакции |
| Обработка отсутствия GUEST | ❌ | **Молча пропускает** — пользователь создаётся без роли |
| Ошибка при отсутствии GUEST | ❌ | **Не существует** — нарушает AC-5 |

### Требуемые изменения

| Изменение | Файл | Причина |
|-----------|------|---------|
| Добавить `GuestRoleNotFoundError` | `roles.errors.ts` | US-01 AC-5: явная ошибка |
| Добавить `GuestRoleMissingError` | `auth.errors.ts` | US-01 AC-5: ошибка регистрации |
| Выбрасывать ошибку при `guestRole === null` | `auth.repository.prisma.ts` | AC-5: отклонять регистрацию |
| Обновить JSDoc в `AuthService.registerUser` | `auth.service.ts` | Синхронизация спецификации |

### Риски

1. **Регрессия существующих тестов**: тесты, ожидающие успешную регистрацию без роли GUEST, могут сломаться. Необходимо обновить тесты в `tests/unit/domains/auth/`.
2. **Тестовое окружение**: `prisma/seed.ts` должен выполняться перед интеграционными тестами, чтобы роль GUEST существовала.
3. **Производительность**: дополнительный `SELECT` роли GUEST внутри транзакции добавляет ~5ms — в пределах NFR AC-1 (~50ms).
