# План реализации: US-02: Защита маршрутов дашборда от неавторизованного доступа

> **Шаблон используется для декомпозиции User Story на технические задачи по слоям архитектуры.**  
> Шаблон создаётся на этапе PLAN (Спецификация) и передаётся в Code-режим для выполнения.  
> Статусы задач: `[TODO]` → `[IN PROGRESS]` → `[DONE]`

---

## 📋 Метаданные

| Параметр       | Значение                          |
| -------------- | --------------------------------- |
| **US-ID**      | `US-02`                           |
| **Название**   | Защита маршрутов дашборда от неавторизованного доступа |
| **Версия плана** | `v1.0`                            |
| **Дата создания** | `2026-07-09`                   |
| **Статус**     | `[TODO]`                          |
| **Зависит от** | `US-01` (автоматическое назначение роли GUEST при регистрации) |

---

## 📎 Ссылки

- **User Story:** [`docs/user-stories/US-02-защита-маршрутов-дашборда-от-неавторизованного-доступа.md`](../user-stories/US-02-защита-маршрутов-дашборда-от-неавторизованного-доступа.md)
- **Требования (REQ):** `docs/requirements/REQ-ROLES-001.md`
- **Модель данных:** `docs/model/entities/user.md`, `docs/model/entities/session.md`

---

## 📁 Дерево файлов

Базовая структура проекта (покрываемые директории):

```
src/
├── middleware.ts                  # ✏️ Изменить: Middleware для защиты /dashboard/* и /api/v1/*
└── app/
    ├── dashboard/
    │   └── layout.tsx             # ✏️ Создать: Layout с проверкой сессии через useSession
    └── (public)/
        └── login/
            └── page.tsx           # ✔ Проверить: существующая страница входа
```

> **Примечание:** Файлы `prisma/schema.prisma`, доменные слои (Types, Validators, Errors, Repository, Service), DI-контейнер и API Route Handlers — **не изменяются**. Защита осуществляется на уровне Middleware и Layout.

---

## 📦 Задачи по слоям

### 0. Middleware (Серверная защита API и UI)

#### Задача 1: Реализовать Middleware для защиты /dashboard/* и /api/v1/*

| Параметр   | Значение                                         |
| ---------- | ------------------------------------------------ |
| **ID**     | `US-02-T0-MW`                                   |
| **Статус** | `[TODO]`                                         |
| **Файлы**  | `src/middleware.ts`                              |
| **Действие** | `Изменить`                                     |

**Целевое состояние:**

- Middleware перехватывает запросы к `/dashboard/:path*` и `/api/v1/:path*`
- Для `/dashboard/*`: если сессия отсутствует — редирект на `/login`
- Для `/api/v1/*`: если сессия отсутствует — возврат `401 Unauthorized`
- Public-роуты (`/login`, `/register`) исключены из защиты

**Чек-лист:**

- [ ] Middleware проверяет `auth()` (или `getServerSession()`) для маршрутов `/dashboard/*`
- [ ] При отсутствии сессии на `/dashboard/*` — редирект на `/login`
- [ ] Для `/api/v1/*` — возврат `401` при отсутствии сессии
- [ ] `/login` и `/register` (и `/api/auth/*`) — исключены из защиты
- [ ] `export const config = { matcher: [...] }` обновлён корректно

---

### 1. UI — Dashboard Layout (Client-side защита)

#### Задача 1: Создать `src/app/dashboard/layout.tsx` с проверкой сессии

| Параметр   | Значение                                         |
| ---------- | ------------------------------------------------ |
| **ID**     | `US-02-T1-LAYOUT`                                |
| **Статус** | `[TODO]`                                         |
| **Файл**   | `src/app/dashboard/layout.tsx`                   |
| **Действие** | `Создать`                                      |

**Целевое состояние:**

- Client Component с `'use client'`
- Использует `useSession()` из `next-auth/react`
- Если сессия не загружена (`status === 'loading'`) — отображает прелоадер
- Если сессия отсутствует (`!session`) — редирект на `/login`
- Если сессия есть — рендерит `children` (вложенные страницы дашборда)

**Чек-лист:**

- [ ] Файл существует как Client Component (`'use client'`)
- [ ] Использует `useSession()` для проверки авторизации
- [ ] При `status === 'loading'` — отображается состояние загрузки
- [ ] При `!session` — `router.push('/login')`
- [ ] При валидной сессии — рендерит `children`
- [ ] JSDoc-аннотации: `@component`, `@category`, `@spec`

---

### 2. Pages — Обновить `src/app/dashboard/page.tsx`

#### Задача 1: Обновить DashboardPage для удаления дублирующей проверки

| Параметр   | Значение                                         |
| ---------- | ------------------------------------------------ |
| **ID**     | `US-02-T2-PAGE`                                  |
| **Статус** | `[TODO]`                                         |
| **Файл**   | `src/app/dashboard/page.tsx`                     |
| **Действие** | `Изменить`                                     |

**Целевое состояние:**

- Убрана дублирующая проверка сессии (`useSession` + `router.push`)
- Компонент предполагает, что доступ гарантирован Layout-ом
- Сохранена структура дашборда (карточки ссылок)
- JSDoc обновлён для отражения нового потока данных

**Чек-лист:**

- [ ] Удалена проверка `!session` и `router.push('/login')` из `useEffect`
- [ ] Удалён `useState(status)` и логика предзагрузки сессии
- [ ] Компонент сразу рендерит контент
- [ ] JSDoc `@data-flow` обновлён: `dashboard/layout.tsx → useSession()`
- [ ] JSDoc `@spec` обновлён: доступ обеспечивается Layout-ом

---

## 📊 Матрица соответствия AC → Задачи

Сопоставление acceptance criteria с задачами плана реализации:

| AC | Описание | Задача | Статус |
|----|----------|--------|--------|
| AC-1 | Редирект гостя на /login при попытке открыть `/dashboard/*` | `US-02-T0-MW` (middleware), `US-02-T1-LAYOUT` (layout) | `[TODO]` |
| AC-2 | Отображение дашборда для авторизованных пользователей | `US-02-T1-LAYOUT` (layout) | `[TODO]` |
| AC-3 | Защита API дашборда (401 Unauthorized для неавторизованных) | `US-02-T0-MW` (middleware) | `[TODO]` |
| EC-1 | У истёкшей сессии — редирект на /login | `US-02-T0-MW`, `US-02-T1-LAYOUT` | `[TODO]` |
| EC-2 | Прямой переход на `/dashboard/plots` — редирект на /login | `US-02-T0-MW`, `US-02-T1-LAYOUT` | `[TODO]` |
| EC-4 | Запрос к API без токена — HTTP 401 | `US-02-T0-MW` | `[TODO]` |
| EC-5 | Ошибка валидации сессии — HTTP 401 + редирект | `US-02-T0-MW`, `US-02-T1-LAYOUT` | `[TODO]` |

---

## ✅ Чек-лист валидации плана

Перед передачей плана в Code-режим убедиться:

### Код и типы
- [ ] Layout имеет JSDoc-аннотации (`@component`, `@category`, `@spec`)
- [ ] Middleware имеет JSDoc-аннотации (`@file`, `@spec`)
- [ ] `src/app/dashboard/page.tsx` имеет обновлённые JSDoc (`@page`, `@auth`, `@spec`, `@data-flow`)

### Целостность
- [ ] Middleware matcher корректно исключает `/api/auth/*`, `/login`, `/register`
- [ ] Layout не вызывает бесконечного редиректа
- [ ] Нет рассинхронизации между L1 (JSDoc) и L2 (User Story)

### Тестирование
- [ ] `npm run type-check` — без ошибок
- [ ] `npm run lint` — без ошибок

### Функциональность
- [ ] AC-1 пройден (ручная проверка: гость → редирект на /login)
- [ ] AC-2 пройден (ручная проверка: авторизованный → дашборд загружается)
- [ ] AC-3 пройден (ручная проверка: API без токена → 401)
- [ ] Нет 500, 404, 400 ошибок в консоли при нормальной работе

---

## 📝 История изменений

| Дата       | Версия | Автор     | Изменение                   |
| ---------- | ------ | --------- | --------------------------- |
| 2026-07-09 | v1.0   | Architect | Создание плана реализации   |

---

## 📎 Дополнительные ссылки

- [`SPECS.md`](../../.roo/rules/SPECS.md) — правила управления спецификациями
- [`MODEL.md`](../../.roo/rules/MODEL.md) — правила управления моделью данных
- [`ARCHITECTURE.md`](../../.roo/rules/ARCHITECTURE.md) — правила архитектуры
- [`PROJECT.md`](../../.roo/rules/PROJECT.md) — глобальные правила проекта
- [`api-paths.md`](../../.roo/rules/api-paths.md) — правила формирования API путей
