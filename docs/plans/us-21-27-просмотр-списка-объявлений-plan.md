# План реализации: US-21-27: Просмотр списка объявлений

> **Шаблон используется для декомпозиции User Story на технические задачи по слоям архитектуры.**  
> Шаблон создаётся на этапе PLAN (Спецификация) и передаётся в Code-режим для выполнения.  
> Статусы задач: `[TODO]` → `[IN PROGRESS]` → `[DONE]`

---

## 📋 Метаданные

| Параметр       | Значение                          |
| -------------- | --------------------------------- |
| **US-ID**      | `US-21-27`                        |
| **Название**   | Просмотр списка объявлений        |
| **Версия плана** | `v1.0`                            |
| **Дата создания** | `2026-07-15`                   |
| **Статус**     | `[DONE]`                   |
| **Зависит от** | `REQ-COMMS-001`                   |

---

## 📎 Ссылки

- **User Story:** [`docs/user-stories/US-21-27-просмотр-списка-объявлений.md`](../../user-stories/US-21-27-просмотр-списка-объявлений.md)
- **Требования (REQ):** `docs/requirements/REQ-COMMS-001.md`
- **Модель данных:** `docs/model/entities/announcement.md`

---

## 📁 Дерево файлов

```
src/
├── domains/
│   └── announcement/                    # Домен объявлений (НОВЫЙ)
│       ├── index.ts                     # Публичный API домена (re-export) (🆕)
│       ├── announcement.types.ts        # Типы сущностей (🆕)
│       ├── announcement.validators.ts   # Zod-схемы валидации (🆕)
│       ├── announcement.errors.ts       # Классы доменных ошибок (🆕)
│       ├── announcement.repository.interface.ts  # Интерфейс репозитория (🆕)
│       ├── announcement.repository.prisma.ts     # Реализация Prisma репозитория (🆕)
│       └── announcement.service.ts      # Бизнес-логика (🆕)
│
├── app/
│   └── api/v1/
│       └── announcements/
│           ├── route.ts             # API endpoint GET /api/v1/announcements (🆕)
│           └── [id]/
│               └── route.ts         # Detail endpoint GET /api/v1/announcements/:id (🆕)
│
├── components/
│   └── features/
│       └── announcement/
│           ├── AnnouncementList.tsx     # Компонент списка объявлений (🆕)
│           ├── AnnouncementCard.tsx     # Компонент карточки объявления (🆕)
│           ├── AnnouncementSearch.tsx   # Компонент поиска (🆕)
│           ├── AnnouncementFilters.tsx  # Компонент фильтров (🆕)
│           └── index.ts                 # Re-export (🆕)
│
└── app/dashboard/
    └── announcements/
        ├── page.tsx                 # Страница списка (🆕)
        └── [id]/
            └── page.tsx             # Страница детали (🆕)
```

---

## 📦 Задачи по слоям

---

### 1. Модель данных (Prisma + DBML)

#### Задача 1: Создать сущность Announcement в модели данных

| Параметр   | Значение                                         |
| ---------- | ------------------------------------------------ |
| **ID**     | `US-21-27-T1`                                    |
| **Статус** | `[TODO]`                                         |
| **Файлы**  | `docs/model/schema.dbml`, `docs/model/entities/announcement.md`, `prisma/schema.prisma` |
| **Действие** | `Создать`                                             |

**Целевое состояние:**

- Создана сущность `Announcement` в концептуальной модели данных
- Создан файл описания сущности `docs/model/entities/announcement.md`
- Обновлена Prisma-схема с сущностью `Announcement`
- Создана миграция для создания таблицы в БД

**Чек-лист:**

- [ ] Обновлён `docs/model/schema.dbml` (добавлена таблица `announcements`)
- [ ] Обновлён `docs/model/entities/announcement.md`
- [ ] Обновлён `prisma/schema.prisma` с использованием `@map()` для snake_case
- [ ] Создана миграция: `npx prisma migrate dev --name "create_announcements_table"`

---

### 2. Types (Доменные типы)

#### Задача 1: Определить интерфейс Announcement для сущности

| Параметр   | Значение                                         |
| ---------- | ------------------------------------------------ |
| **ID**     | `US-21-27-T2-1`                                  |
| **Статус** | `[TODO]`                                         |
| **Файл**   | `src/domains/announcement/announcement.types.ts` |
| **Действие** | `Создать`                                       |

**Целевое состояние:**

- Определён интерфейс `Announcement` для полного объекта объявления
- Определены типы `AnnouncementListResponse`, `AnnouncementDetailResponse`
- Определён тип `AnnouncementFilter` для фильтрации списка

**Чек-лист:**

- [ ] Типы определены с JSDoc-аннотациями (`@type`, `@domain`, `@spec`)
- [ ] Все поля описаны с комментариями
- [ ] Указаны инварианты и констрейнты

---

### 3. Validators (Zod-схемы)

#### Задача 1: Создать Zod-схемы для фильтрации и пагинации

| Параметр   | Значение                                         |
| ---------- | ------------------------------------------------ |
| **ID**     | `US-21-27-T3-1`                                  |
| **Статус** | `[TODO]`                                         |
| **Файл**   | `src/domains/announcement/announcement.validators.ts` |
| **Действие** | `Создать`                                       |

**Целевое состояние:**

- Создана схема `AnnouncementListQuerySchema` для валидации query параметров
- Создана схема `AnnouncementStatusSchema` для валидации статуса

**Чек-лист:**

- [ ] Схемы валидированы с JSDoc-аннотациями (`@schema`, `@domain`, `@spec`)
- [ ] Валидация охватывает все параметры запроса
- [ ] Сообщения об ошибках на русском языке

---

### 4. Errors (Доменные ошибки)

#### Задача 1: Создать классы доменных ошибок

| Параметр   | Значение                                         |
| ---------- | ------------------------------------------------ |
| **ID**     | `US-21-27-T4`                                    |
| **Статус** | `[TODO]`                                         |
| **Файл**   | `src/domains/announcement/announcement.errors.ts` |
| **Действие** | `Создать`                                       |

**Целевое состояние:**

- Создан класс `AnnouncementNotFoundError` наследуется от `NotFoundError`
- Создан класс `AnnouncementInvalidDataError` наследуется от `ValidationError`

**Чек-лист:**

- [ ] Классы ошибок наследуются от базовых классов из `src/shared/errors/`
- [ ] JSDoc: `@error`, `@domain`, `@description`
- [ ] Сообщения об ошибках на русском языке
- [ ] HTTP-статус указан в конструкторе

---

### 5. Repository (Интерфейс + Реализация)

#### Задача 1: Создать интерфейс репозитория

| Параметр   | Значение                                         |
| ---------- | ------------------------------------------------ |
| **ID**     | `US-21-27-T5-1`                                  |
| **Статус** | `[TODO]`                                         |
| **Файл**   | `src/domains/announcement/announcement.repository.interface.ts` |
| **Действие** | `Создать`                                       |

**Целевое состояние:**

- Определён интерфейс `IAnnouncementRepository`
- Определены методы:
  - `findAll(page, limit, filters)`: найти все объявления с пагинацией
  - `findById(id)`: найти объявление по ID

**Чек-лист:**

- [ ] Интерфейс: JSDoc `@interface`, `@domain`, `@spec`
- [ ] Методы: `@param`, `@returns`, `@throws`
- [ ] `findById` возвращает `null` если не найден (НЕ бросает ошибку)

#### Задача 2: Создать реализацию репозитория (Prisma)

| Параметр   | Значение                                         |
| ---------- | ------------------------------------------------ |
| **ID**     | `US-21-27-T5-2`                                  |
| **Статус** | `[TODO]`                                         |
| **Файл**   | `src/domains/announcement/announcement.repository.prisma.ts` |
| **Действие** | `Создать`                                       |

**Целевое состояние:**

- Реализован класс `AnnouncementRepositoryPrisma`, реализующий `IAnnouncementRepository`
- Все методы используют Prisma Client
- Данные возвращаются в форматах доменных типов

**Чек-лист:**

- [ ] Prisma: используются доменные типы, а не сырые данные
- [ ] Обработка ошибок через доменные ошибки
- [ ] Пагинация реализована через `skip` и `take` в Prisma

---

### 6. Service (Бизнес-логика)

#### Задача 1: Создать сервис объявления

| Параметр   | Значение                                         |
| ---------- | ------------------------------------------------ |
| **ID**     | `US-21-27-T6`                                    |
| **Статус** | `[TODO]`                                         |
| **Файл**   | `src/domains/announcement/announcement.service.ts` |
| **Действие** | `Создать`                                       |

**Целевое состояние:**

- Создан класс `AnnouncementService` с бизнес-логикой
- Реализован метод `getAnnouncementsList(page, limit, filters)`
- Реализован метод `incrementViewCount(id)`

**Чек-лист:**

- [ ] JSDoc: `@service`, `@domain`, `@param`, `@returns`, `@throws`, `@spec`
- [ ] Валидация входных данных через Zod-схемы
- [ ] Бизнес-валидация через доменные ошибки
- [ ] Вызов репозитория через DI (интерфейс, не Prisma напрямую)
- [ ] Методы не превышают 50 строк

---

### 7. DI Container (Зависимости)

#### Задача 1: Зарегистрировать сервис в контейнере

| Параметр   | Значение                                         |
| ---------- | ------------------------------------------------ |
| **ID**     | `US-21-27-T7`                                    |
| **Статус** | `[TODO]`                                         |
| **Файл**   | `src/di/container.ts`                            |
| **Действие** | `Обновить`                                      |

**Целевое состояние:**

- Создана фабрика `createAnnouncementService` в контейнере
- Зарегистрированы зависимости: `IAnnouncementRepository`

**Чек-лист:**

- [ ] Фабрика зарегистрирована в контейнере
- [ ] Используется singleton-паттерн (если применимо)
- [ ] Экспортирована публичная функция `createAnnouncementService()`

---

### 8. API Route Handlers

#### Задача 1: Создать endpoint для списка объявлений

| Параметр   | Значение                                         |
| ---------- | ------------------------------------------------ |
| **ID**     | `US-21-27-T8-1`                                  |
| **Статус** | `[TODO]`                                         |
| **Файл**   | `src/app/api/v1/announcements/route.ts`          |
| **Действие** | `Создать`                                       |

**Целевое состояние:**

- Создан endpoint `GET /api/v1/announcements`
- Endpoint возвращает пагинированный список объявлений
- Поддерживаются query параметры: `page`, `limit`, `status`, `search`

**Чек-лист:**

- [ ] JSDoc: `@route`, `@auth`, `@body`, `@response`, `@spec`
- [ ] Используется `auth()` для проверки авторизации
- [ ] Валидация query параметров через Zod-схемы
- [ ] Обработка ошибок через `instanceof BaseError`
- [ ] Возврат стандартизированного ответа: `{ success: true/false, data?, error? }`
- [ ] Соответствующие HTTP-статусы (200, 400, 401, 404, 500)

#### Задача 2: Создать endpoint для деталей объявления

| Параметр   | Значение                                         |
| ---------- | ------------------------------------------------ |
| **ID**     | `US-21-27-T8-2`                                  |
| **Статус** | `[TODO]`                                         |
| **Файл**   | `src/app/api/v1/announcements/[id]/route.ts`     |
| **Действие** | `Создать`                                       |

**Целевое состояние:**

- Создан endpoint `GET /api/v1/announcements/:id`
- Endpoint возвращает полную информацию об объявлении
- При успешном получении увеличивается счётчик просмотров

**Чек-лист:**

- [ ] JSDoc: `@route`, `@auth`, `@response`, `@spec`
- [ ] Используется `auth()` для проверки авторизации
- [ ] Обработка ошибок через `instanceof BaseError`
- [ ] При 404 возвращается `AnnouncementNotFoundError`
- [ ] Вызывается `service.incrementViewCount()` после получения объявления

---

### 9. UI-компоненты

#### Задача 1: Компонент списка объявлений

| Параметр   | Значение                                         |
| ---------- | ------------------------------------------------ |
| **ID**     | `US-21-27-T9-1`                                  |
| **Статус** | `[TODO]`                                         |
| **Файл**   | `src/components/features/announcement/AnnouncementList.tsx` |
| **Действие** | `Создать`                                       |

**Целевое состояние:**

- Создан компонент `AnnouncementList` для отображения списка объявлений
- Компонент отображает пагинированный список с карточками объявлений
- Поддерживает состояние `loading`, `error`, `empty`

**Чек-лист:**

- [ ] JSDoc: `@component`, `@category`, `@spec`
- [ ] Директива `'use client'` наверху файла
- [ ] Обработаны состояния: `loading`, `error`, `empty`, `success`
- [ ] Используется `apiClient` для запросов
- [ ] Пустое состояние отображается через `EmptyState`
- [ ] Используется `useCallback` для всех обработчиков событий

#### Задача 2: Компонент карточки объявления

| Параметр   | Значение                                         |
| ---------- | ------------------------------------------------ |
| **ID**     | `US-21-27-T9-2`                                  |
| **Статус** | `[TODO]`                                         |
| **Файл**   | `src/components/features/announcement/AnnouncementCard.tsx` |
| **Действие** | `Создать`                                       |

**Целевое состояние:**

- Создан компонент `AnnouncementCard` для отображения одного объявления
- Отображается заголовок, автор, дата, статус, бейдж "Важно"

**Чек-лист:**

- [ ] JSDoc: `@component`, `@category`, `@spec`
- [ ] Директива `'use client'` наверху файла
- [ ] Отображение бейджа "Важно" для важных объявлений
- [ ] Бейджи статуса (Published, Draft, Archived)

#### Задача 3: Компонент поиска

| Параметр   | Значение                                         |
| ---------- | ------------------------------------------------ |
| **ID**     | `US-21-27-T9-3`                                  |
| **Статус** | `[TODO]`                                         |
| **Файл**   | `src/components/features/announcement/AnnouncementSearch.tsx` |
| **Действие** | `Создать`                                       |

**Целевое состояние:**

- Создан компонент `AnnouncementSearch` для поиска объявлений
- Поддерживает ввод текста для фильтрации по заголовку

**Чек-лист:**

- [ ] JSDoc: `@component`, `@category`, `@spec`
- [ ] Debounce для поиска (500ms)