# План реализации: US-04: Реализация страницы входа (Login)

> **Шаблон используется для декомпозиции User Story на технические задачи по слоям архитектуры.**  
> Шаблон создаётся на этапе PLAN (Спецификация) и передаётся в Code-режим для выполнения.  
> Статусы задач: `[TODO]` → `[IN PROGRESS]` → `[DONE]`

---

## 📋 Метаданные

| Параметр       | Значение                          |
| -------------- | --------------------------------- |
| **US-ID**      | `US-04`                           |
| **Название**   | Реализация страницы входа (Login) |
| **Версия плана** | `v1.1`                            |
| **Дата создания** | `2026-07-09`                   |
| **Дата обновления** | `2026-07-09`                |
| **Статус**     | `[DONE]`                          |
| **Зависит от** | `US-01` (автоматическое назначение роли GUEST), `US-03` (защита маршрутов) |

---

## 📎 Ссылки

- **User Story:** [`docs/user-stories/US-04-реализация-страницы-входа-login.md`](../../user-stories/US-04-реализация-страницы-входа-login.md)
- **Требования (REQ):** `docs/requirements/REQ-AUTH-001.md`
- **Модель данных:** `docs/model/entities/user.md`

---

## 📁 Дерево файлов

Базовая структура проекта (покрываемые директории):

```
src/
├── components/
│   └── features/
│       └── auth/
│           └── LoginForm/
│               ├── LoginForm.tsx         # 🆕 Компонент формы входа
│               └── index.ts              # 🆕 Re-export
│
└── app/(public)/login/
    ├── page.tsx                      # ✏️ Страница входа
    └── page.client.tsx               # 🆕 Клиентский компонент формы
```

---

## 📦 Задачи по слоям

---

### 1. Модель данных (Prisma + DBML)

#### Задача: Проверить модель данных User

| Параметр   | Значение                                         |
| ---------- | ------------------------------------------------ |
| **ID**     | `US-04-T1`                                       |
| **Статус** | `[DONE]`                                         |
| **Файлы**  | `docs/model/entities/user.md`, `prisma/schema.prisma` |
| **Действие** | `Проверить` |

**Целевое состояние:**

- Модель `User` содержит поля `email`, `password` (хеш)
- Поле `email` имеет уникальный индекс

**Чек-лист:**

- [x] Поля `email`, `passwordHash` существуют в `User`
- [x] `email` имеет `@unique` индекс
- [x] `password` хранит bcrypt хеш

---

### 2. Types (Доменные типы)

#### Задача: Проверить доменные типы auth

| Параметр   | Значение                                         |
| ---------- | ------------------------------------------------ |
| **ID**     | `US-04-T2`                                       |
| **Статус** | `[DONE]`                                         |
| **Файл**   | `src/domains/auth/auth.types.ts`                 |
| **Действие** | `Проверить` |

**Целевое состояние:**

- Определены типы `LoginData`, `UserData`
- Типы имеют JSDoc-аннотации

**Чек-лист:**

- [x] `LoginData` определён с полями `email`, `password`
- [x] `UserData` определён с полями `id`, `email`, `name`, `createdAt`, `updatedAt`
- [x] Все типы имеют JSDoc-аннотации (`@type`, `@domain`, `@spec`)

---

### 3. Validators (Zod-схемы)

#### Задача: Проверить Zod-схему для входа

| Параметр   | Значение                                           |
| ---------- | -------------------------------------------------- |
| **ID**     | `US-04-T3`                                         |
| **Статус** | `[DONE]`                                           |
| **Файл**   | `src/domains/auth/auth.validators.ts`              |
| **Действие** | `Проверить` |

**Целевое состояние:**

- Определена `loginSchema` с валидацией `email` и `password`
- Схема имеет JSDoc-аннотации

**Чек-лист:**

- [x] `loginSchema` валидирует `email` (format, required) и `password` (required)
- [x] `email` приводится к `lowercase` и обрезается `trim`
- [x] Сообщения об ошибках на русском языке
- [x] Тип `LoginInput` определён через `z.infer`

---

### 4. Errors (Доменные ошибки)

#### Задача: Проверить классы ошибок auth

| Параметр   | Значение                                         |
| ---------- | ------------------------------------------------ |
| **ID**     | `US-04-T4`                                       |
| **Статус** | `[DONE]`                                         |
| **Файл**   | `src/domains/auth/auth.errors.ts`                |
| **Действие** | `Проверить` |

**Целевое состояние:**

- Определён `InvalidCredentialsError` для ошибок входа

**Чек-лист:**

- [x] `InvalidCredentialsError` наследуется от `UnauthorizedError` — HTTP 401
- [x] Сообщение: "Неверный email или пароль"
- [x] Класс снабжён JSDoc-аннотацией (`@error`, `@domain`)

---

### 5. Repository (Интерфейс + Реализация)

#### Задача: Проверить метод findByEmailWithPassword

| Параметр   | Значение                                             |
| ---------- | ---------------------------------------------------- |
| **ID**     | `US-04-T5`                                           |
| **Статус** | `[DONE]`                                             |
| **Файл**   | `src/domains/auth/auth.repository.prisma.ts`         |
| **Действие** | `Проверить` |

**Целевое состояние:**

- Метод `findByEmailWithPassword` возвращает пользователя с хешем пароля

**Чек-лист:**

- [x] Метод `findByEmailWithPassword` определён в `IAuthRepository`
- [x] Метод возвращает `UserWithPassword` или `null`
- [x] Реализация в `AuthRepository` корректно запрашивает поле `password`
- [x] Метод имеет JSDoc-аннотации

---

### 6. Service (Бизнес-логика)

#### Задача: Проверить метод verifyCredentials

| Параметр   | Значение                                  |
| ---------- | ----------------------------------------- |
| **ID**     | `US-04-T6`                                |
| **Статус** | `[DONE]`                                  |
| **Файл**   | `src/domains/auth/auth.service.ts`        |
| **Действие** | `Проверить` |

**Целевое состояние:**

- Метод `verifyCredentials` реализует логику входа:
  - Валидация через `loginSchema`
  - Поиск пользователя через `findByEmailWithPassword`
  - Сравнение пароля через `bcrypt.compare`
  - Возврат `UserData` без пароля
  - Общий message для всех ошибок входа

**Чек-лист:**

- [x] Метод определён с JSDoc-аннотациями (`@service`, `@param`, `@returns`, `@throws`, `@spec`)
- [x] Валидация входных данных через `loginSchema`
- [x] При отсутствии пользователя или неверном пароле выбрасывается `InvalidCredentialsError`
- [x] Пароль никогда не возвращается в ответе

---

### 7. DI Container

#### Задача: Проверить фабрику AuthService

| Параметр   | Значение                           |
| ---------- | ---------------------------------- |
| **ID**     | `US-04-T7`                         |
| **Статус** | `[DONE]`                           |
| **Файл**   | `src/di/container.ts`              |
| **Действие** | `Проверить` |

**Целевое состояние:**

- Фабрика `createAuthService` зарегистрирована в DI-контейнере

**Чек-лист:**

- [x] `createAuthService` экспортирована из `container.ts`
- [x] Метод возвращает экземпляр `AuthService` с внедрённым `IAuthRepository`

---

### 8. API / NextAuth Инфраструктура

#### Задача: Проверить конфигурацию NextAuth

| Параметр   | Значение                                              |
| ---------- | ----------------------------------------------------- |
| **ID**     | `US-04-T8`                                            |
| **Статус** | `[DONE]`                                              |
| **Файл**   | `src/infrastructure/auth/auth.config.ts`, `src/lib/auth.ts` |
| **Действие** | `Проверить` |

**Целевое состояние:**

- `CredentialsProvider` настроен в `auth.config.ts`
- `jwt` и `session` callback-ы передают `id` и `roles`
- Страница входа указана как `signIn: '/login'`

**Чек-лист:**

- [x] `providers` экспортированы из `auth.config.ts`
- [x] `authorize` метод вызывает `AuthService.verifyCredentials`
- [x] `jwt` callback добавляет `id` и `roles` в токен
- [x] `session` callback передаёт данные в сессию
- [x] `getServerSession` экспортирован как `auth()` из `lib/auth.ts`

> **Примечание:** Для страницы входа используется встроенный механизм `next-auth` с `signIn` (не кастомный `/api/v1/auth/login` endpoint).

---

### 9. UI Components (Фича-компоненты)

#### Задача: Создать LoginForm компонент

| Параметр   | Значение                                                     |
| ---------- | ------------------------------------------------------------ |
| **ID**     | `US-04-T9`                                                   |
| **Статус** | `[DONE]`                                                     |
| **Файлы**   | `src/components/features/auth/LoginForm/LoginForm.tsx`, `src/components/features/auth/LoginForm/index.ts`                 |
| **Действие** | `Создать` |

**Целевое состояние:**

- Клиентский компонент формы входа с полями `email` и `password`
- Клиентская валидация полей
- Обработка отправки формы через `signIn` из `next-auth`
- Управление состояниями: `isLoading`, `error`
- Блокировка кнопки при `isLoading`
- Доступность: `aria-label`, `focus-ring`

**Чек-лист:**

- [x] Компонент определён с JSDoc-аннотациями (`@component`, `@category`, `@spec`)
- [x] Директива `'use client'` наверху файла
- [x] Обработаны состояния: `isLoading`, `error`, `success`
- [x] Используется `signIn` из `next-auth` для аутентификации
- [x] Кнопка блокируется при `isLoading`
- [x] Доступность: `aria-label` для полей и кнопки
- [x] Поля имеют `required` атрибуты и валидацию HTML5
- [x] Ошибки отображаются на русском языке

**Интерфейс компонента:**

```typescript
export interface LoginFormProps {
  /** URL для перенаправления после успешного входа */
  callbackUrl?: string;
  /** Флаг для отображения flash-сообщения после регистрации */
  showRegistrationMessage?: boolean;
}
```

---

### 10. Pages (Страницы)

#### Задача: Обновить страницу /login

| Параметр   | Значение                                   |
| ---------- | ------------------------------------------ |
| **ID**     | `US-04-T10`                                |
| **Статус** | `[DONE]`                                   |
| **Файлы**   | `src/app/(public)/login/page.tsx`, `src/app/(public)/login/page.client.tsx`          |
| **Действие** | `Обновить` |

**Целевое состояние:**

- Страница делегирует рендеринг формы компоненту `LoginForm`
- URL search параметры (`callbackUrl`, `registered`) передаются в `LoginForm`
- Страница не требует авторизации (публичная)
- Проверка сессии на сервере для редиректа авторизованных пользователей

**Чек-лист:**

- [x] Страница имеет JSDoc-аннотации (`@page`, `@auth`, `@spec`, `@data-flow`)
- [x] Директива `'use client'` наверху файла
- [x] `useSearchParams` извлекает `callbackUrl` и `registered`
- [x] Параметры передаются в клиентский компонент формы
- [x] Создание `page.client.tsx` для клиентской обёртки
- [x] Проверка сессии на сервере для редиректа авторизованных пользователей

---

## 📊 Матрица соответствия AC → Задачи

| AC | Описание | Задача | Статус |
|----|----------|--------|--------|
| AC-1.1 | Отображение формы входа | US-04-T9 | `[DONE]` |
| AC-1.2 | Поля email и password | US-04-T9 | `[DONE]` |
| AC-1.3 | Валидация на стороне клиента | US-04-T9 | `[DONE]` |
| AC-2.1 | Создание сессии при успехе | US-04-T8 | `[DONE]` |
| AC-2.2 | Редирект на /dashboard | US-04-T10 | `[DONE]` |
| AC-3.1 | Ошибка валидации email | US-04-T9 | `[DONE]` |
| AC-4.1 | Ошибка "Неверный email или пароль" | US-04-T6 | `[DONE]` |
| AC-4.2 | Форма остаётся доступной | US-04-T9 | `[DONE]` |
| AC-5.1 | Редирект авторизованного пользователя | US-03 | `[DONE]` |
| AC-6.1 | Валидация на сервере | US-04-T3 | `[DONE]` |

---

## ✅ Чек-лист валидации плана

Перед передачей плана в Code-режим убедиться:

### Код и типы
- [x] Все типы имеют JSDoc-аннотации (`@type`, `@domain`, `@spec`)
- [x] Все интерфейсы репозиториев имеют JSDoc-аннотации (`@interface`, `@spec`)
- [x] Все методы сервисов имеют JSDoc-аннотации (`@service`, `@param`, `@returns`, `@throws`, `@spec`)
- [x] Все API Route Handlers имеют JSDoc-аннотации (`@route`, `@auth`, `@response`, `@spec`)
- [x] Все UI-компоненты имеют JSDoc-аннотации (`@component`, `@category`, `@spec`) — `LoginForm` создан
- [x] Все страницы имеют JSDoc-аннотации (`@page`, `@auth`, `@spec`, `@data-flow`)

### Целостность
- [x] `prisma/schema.prisma` синхронизирован с `docs/model/schema.dbml`
- [x] Миграции созданы и применены: `npx prisma migrate dev`
- [x] DI-контейнер (`src/di/container.ts`) обновлён с фабриками
- [x] Ре-экспорты в `src/domains/auth/index.ts` актуальны
- [x] Нет рассинхронизации между L1 (JSDoc) и L2 (User Story)

---

## 🔄 История изменений

| Версия | Дата | Автор | Изменение |
|--------|------|-------|-----------|
| v1.0 | 2026-07-09 | Architect | Создание плана реализации US-04 |
| v1.1 | 2026-07-09 | Code | Реализация US-04: T9 (LoginForm), T10 (page.tsx, page.client.tsx) |
