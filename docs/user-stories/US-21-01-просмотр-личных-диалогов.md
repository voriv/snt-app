# US-21-01: Просмотр списка личных диалогов

> Эпик: Система общения в СНТ (REQ-COMMS-001)

---

## Формулировка

**Как** зарегистрированный пользователь СНТ,
**я хочу** просматривать список своих личных диалогов,
**чтобы** быстро находить и переходить к нужным беседам.

---

## Целевое состояние

После реализации пользователь сможет открыть страницу `/dashboard/messages` и увидеть список своих личных диалогов в виде карточек. Каждая карточка показывает: аватар собеседника, имя собеседника, превью последнего сообщения, время последнего сообщения, счётчик непрочитанных. Переход по карточке открывает диалог на `/dashboard/messages/:conversationId`.

---

## Контекст

### Проблема

В системе отсутствует инструмент для просмотра списка личных диалогов. Пользователи не могут увидеть, с кем у них есть активные беседы, и не могут быстро переключаться между ними.

### Решение

Создать страницу `/dashboard/messages` со списком диалогов в виде карточек, загружаемых через `GET /api/v1/conversations`. Диалоги сортируются по времени последнего сообщения (новые сверху). Поиск по имени собеседника фильтрует список на клиенте.

### Границы

- **Входит в US:** Отображение списка диалогов в виде карточек, превью последнего сообщения, счётчик непрочитанных, поиск по имени собеседника (клиентский), переход к диалогу, бейдж непрочитанных в навбаре.
- **Явно исключено:** Отправка сообщений (US-21-03), начало нового диалога (US-21-02), групповые чаты (US-21-04..US-21-09).

---

## Поведение (User Behavior)

### B-1: Открытие страницы сообщений

Пользователь кликает на ссылку «Сообщения» в навбаре (или переходи по `/dashboard/messages`). Загружается страница с индикатором «Загрузка диалогов...». При успешной загрузке отображается список карточек диалогов. При отсутствии диалогов — `EmptyState` с кнопкой «Написать сообщение».

### B-2: Навигация по карточке диалога

Пользователь видит карточки диалогов, отсортированные по времени последнего сообщения (новые сверху). Клик на карточку — переход на страницу диалога `/dashboard/messages/:conversationId`. На карточке отображается аватар собеседника, имя, превью последнего сообщения (60 символов), время и счётчик непрочитанных (если > 0).

### B-3: Поиск по диалогам

Пользователь вводит текст в поле поиска на странице `/dashboard/messages`. При вводе минимум 2 символов список фильтруется по имени собеседника на стороне клиента (debounce 300ms). Если поиск не дал результатов — `EmptyState` с текстом «Ничего не найдено».

### B-4: Отображение бейджа непрочитанных в навбаре

При наличии непрочитанных сообщений на ссылке «Сообщения» в навбаре отображается бейдж с общим количеством непрочитанных сообщений. Бейдж обновляется при загрузке страницы.

---

## Функциональные требования

### FR-REQ-COMMS-001-04: Отображение списка диалогов

Система должна отображать список активных диалогов пользователя на странице `/dashboard/messages`.

#### AC-1.1: Загрузка списка диалогов

- **Дано:** Пользователь авторизован
- **Когда:** Пользователь открывает `/dashboard/messages`
- **Тогда:** Список диалогов загружается через `GET /api/v1/conversations` и отображается в виде карточек

#### AC-1.2: Структура карточки диалога

- **Дано:** Диалоги успешно загружены
- **Когда:** Карточка рендерится
- **Тогда:** Карточка содержит: аватар собеседника, имя собеседника, превью последнего сообщения (обрезанное до 60 символов), время последнего сообщения, бейдж непрочитанных (если > 0)

#### AC-1.3: Переход к диалогу

- **Дано:** Пользователь видит список диалогов
- **Когда:** Пользователь кликает на карточку
- **Тогда:** Пользователь переходит на страницу диалога `/dashboard/messages/:conversationId`

#### AC-1.4: Сортировка по времени

- **Дано:** Загружено несколько диалогов
- **Когда:** Список рендерится
- **Тогда:** Диалоги отсортированы по времени последнего сообщения (новые сверху)

### FR-REQ-COMMS-001-05: Поиск по диалогам

Система должна поддерживать поиск по диалогам (по имени собеседника).

#### AC-1.5: Поиск на клиенте

- **Дано:** Пользователь вводит текст в поле поиска
- **Когда:** Введено минимум 2 символа
- **Тогда:** Список фильтруется по имени собеседника (debounce 300ms, фильтрация на клиенте)

### FR-REQ-COMMS-001-06: Счётчик непрочитанных

Система должна отображать счётчик непрочитанных сообщений в навбаре.

#### AC-1.6: Бейдж в навбаре

- **Дано:** У пользователя есть непрочитанные сообщения
- **Когда:** Рендерится навбар
- **Тогда:** На ссылке «Сообщения» отображается бейдж с общим количеством непрочитанных

---

## Бизнес-правила и валидация

| ID | Правило | Описание | Валидация |
|----|---------|----------|-----------|
| BR-01 | Авторизация | Только авторизованные пользователи могут просматривать диалоги | `auth()` в API, `useSession()` на странице |
| BR-02 | Приватность | Личные сообщения видны только участникам диалога | Фильтрация на уровне Repository по `conversation_participants` |
| BR-10 | Удалённый пользователь | При удалении пользователя сообщения сохраняются, отправитель — «Удалённый пользователь» | Проверка `userId === null` при рендере |

---

## Граничные случаи (Edge Cases)

| # | Ситуация | Ссылка на REQ | Ожидаемое поведение |
|---|----------|---------------|---------------------|
| 1 | Нет диалогов | EC-07 (Список диалогов пуст) | Отображается `EmptyState` с текстом «У вас пока нет диалогов» и кнопкой «Написать сообщение» → переход на US-21-02 |
| 2 | Собеседник заблокирован | BR-27 + EC-23 | Диалог с заблокированным пользователем скрыт из списка (или отображается с пометкой) |
| 3 | Собеседник удалён | BR-10 + EC-12 | Вместо имени отображается «Удалённый пользователь», аватар — заглушка |
| 4 | Медленная сеть | EC-10 | Отображается индикатор загрузки с текстом «Загрузка диалогов...» |
| 5 | Ошибка загрузки (сеть/API) | EC-10 | Сообщение об ошибке с кнопкой «Повторить» |

---

## Модель данных

| Сущность | Описание | Статус |
|----------|----------|--------|
| Conversation | Личный диалог между двумя пользователями | Новая |
| ConversationParticipant | Связь пользователь-диалог | Новая |
| Message | Сообщение в диалоге/чате | Новая |

---

## Влияние на слои архитектуры

### Модель данных

- [ ] Новые сущности: `conversations`, `conversation_participants`, `messages`
- [ ] См. детальное описание в REQ-COMMS-001 §8

### Types

- [ ] Новый файл `src/domains/comms/comms.types.ts`:
  - `ConversationListItem` — `{ conversationId: string, participantId: string, participantName: string, participantAvatar?: string, lastMessagePreview?: string, lastMessageAt: Date, unreadCount: number }`

### Validators

- [ ] Новый файл `src/domains/comms/comms.validators.ts`:
  - `getConversationsQuerySchema` — валидация query-параметров (опционально: `search`, `limit`)

### Errors

- [ ] Новый файл `src/domains/comms/comms.errors.ts`:
  - `CommsAccessForbiddenError` — наследуется от `ForbiddenError`, код `COMMS_ACCESS_FORBIDDEN`

### Repository

- [ ] Новый файл `src/domains/comms/comms.repository.interface.ts`:
  - `ICommsRepository.getUserConversations(userId)` — `Promise<ConversationListItem[]>`
- [ ] Новый файл `src/domains/comms/comms.repository.prisma.ts`:
  - Prisma-реализация с JOIN `conversations` + `conversation_participants` + `messages` + `users`

### Service

- [ ] Новый файл `src/domains/comms/comms.service.ts`:
  - `CommsService.getUserConversations(userId)` — `Promise<ConversationListItem[]>`
  - Валидация входных параметров через Zod

### DI

- [ ] Регистрация `CommsService` в `src/di/container.ts`
- [ ] Фабрика `createCommsService()`

### API

| Метод | Путь | Описание | Авторизация |
|-------|------|----------|-------------|
| GET | `/api/v1/conversations` | Список личных диалогов текущего пользователя | Required |

### UI

| Компонент | Тип | Описание |
|-----------|-----|----------|
| `ConversationList` | Feature | Список карточек диалогов с полем поиска и состояниями loading/error/empty |
| `ConversationCard` | Feature | Карточка одного диалога: аватар, имя, превью, время, бейдж |

### Pages

| Страница | Маршрут | Описание |
|----------|---------|----------|
| `MessagesPage` | `/dashboard/messages` | Страница списка личных диалогов |

---

## Нефункциональные требования

- **Безопасность:** Доступ к `/dashboard/messages` и `GET /api/v1/conversations` требует авторизации. Диалоги видны только их участникам.
- **Производительность:** Загрузка списка диалогов: < 500ms при до 500 записей.
- **Доступность:** Карточки доступны с клавиатуры (tab-навигация). Бейджи непрочитанных имеют ARIA-метки.
- **Локализация:** Все сообщения на русском языке.
- **Адаптивность:** Карточки корректно отображаются на мобильных устройствах.

---

## Открытые вопросы

| # | Вопрос | Допущение |
|---|--------|-----------|
| 1 | Как обрабатывать очень длинные имена собеседников? | Обрезать текст с ellipsis в карточке |
| 2 | Нужно ли показывать статус «онлайн» собеседника? | Не показывать на данном этапе |

---

## Связанные User Stories

| US | Название | Связь |
|----|----------|-------|
| US-21-02 | Начало нового личного диалога | Кнопка «Написать сообщение» из EmptyState |
| US-21-03 | Отправка личных сообщений | Переход из карточки диалога |
| US-21-35 | Индикатор непрочитанных уведомлений в навбаре | Бейдж в навбаре |

---

## История

| Дата | Автор | Действие |
|------|-------|----------|
| 2026-07-13 | ИИ-архитектор | Создание |
| 2026-07-15 | ИИ-архитектор | Добавлен раздел «Поведение (User Behavior)», приведение к шаблону US |
