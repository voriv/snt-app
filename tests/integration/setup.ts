/**
 * @file setup.ts
 * @description Настройка перед каждым интеграционным тестом
 *
 * @spec
 * - Экспортирует прима клиент для использования в тестах
 * - Использует тот же singleton что и приложение для корректной видимости транзакций
 * - Тесты изолируются через beforeEach cleanup в каждом файле
 * - Системные данные создаются в global-setup.ts и сохраняются
 */

// Импортируем singleton PrismaClient из приложения
// Это гарантирует что тесты и репозитории используют одно подключение к БД
import { prisma } from '@/infrastructure/prisma/client';

// Экспортируем prisma для использования в тестах
export { prisma };
