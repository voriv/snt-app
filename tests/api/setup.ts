/**
 * @file tests/api/setup.ts
 * @description Глобальные моки и настройки для API тестов
 *
 * @spec
 * - Не содержит моков для auth и container - каждый тест настраивает свои моки через vi.hoisted()
 * - Все моки задаются в каждом тесте отдельно для полного контроля
 */
import { vi } from 'vitest';

// Глобальные моки НЕ используются - каждый тест настраивает свои моки через vi.hoisted()
// Это предотвращает конфликты между тестами и setup.ts

// Пустой setup - моки задаются в каждом тесте отдельно
