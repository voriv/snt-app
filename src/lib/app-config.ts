/**
 * Конфигурация приложения
 * Все константы, относящиеся к названию и основным настройкам приложения,
 * определяются здесь для удобного управления.
 */

/**
 * Название приложения
 * Используется в title, описаниях, шапках страниц и т.д.
 */
export const APP_NAME = process.env.NEXT_PUBLIC_APP_NAME || 'СНТ-Приложение';

/**
 * Описание приложения
 * Используется в meta-тегах, футерах, заголовках страниц
 */
export const APP_DESCRIPTION = 
  process.env.NEXT_PUBLIC_APP_DESCRIPTION || 'Управление садоводческим товариществом';
