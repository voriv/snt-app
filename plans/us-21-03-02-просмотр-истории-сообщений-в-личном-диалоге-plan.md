# План реализации: US-21-03-02: Просмотр истории сообщений в личном диалоге

> **Шаблон используется для декомпозиции User Story на технические задачи по слоям архитектуры.**
> Шаблон создаётся на этапе PLAN (Спецификация) и передаётся в Code-режим для выполнения.
> Статусы задач: `[TODO]` → `[IN PROGRESS]` → `[DONE]`

---

## 📋 Метаданные

| Параметр       | Значение                          |
| -------------- | --------------------------------- |
| **US-ID**      | `US-21-03-02`                     |
| **Название**   | Просмотр истории сообщений в личном диалоге |
| **Версия плана** | `v1.0`                            |
| **Дата создания** | `2026-07-14`                   |
| **Статус**     | `[DONE]`                   |
| **Зависит от** | `US-21-01` (список диалогов), `US-21-02` (начало диалога) |

---

## 📎 Ссылки

- **User Story:** [`docs/user-stories/US-21-03-02-просмотр-истории-сообщений-в-личном-диалоге.md`](../../docs/user-stories/US-21-03-02-просмотр-истории-сообщений-в-личном-диалоге.md)
- **Требования (REQ):** `docs/requirements/REQ-COMMS-001.md` (FR-REQ-COMMS-001-03, FR-REQ-COMMS-001-02)
- **Модель данных:** `docs/model/entities/message.md`, `docs/model/entities/conversation.md`

---

## 📁 Дерево файлов

Базовая структура проекта (покрываемые директории):

```
src/
├── domains/
│   └── comms/
│       ├── index.ts                      ✏️ Re-export (добавить новые типы)
│       ├── comms.types.ts                ✏️ Добавить MessageListItem, ConversationMessagesResponse
│       ├── comms.validators.ts           ✏️ Добавить getMessageListQuerySchema, sendMessageSchema
│       ├── comms.errors.ts               ✏️ Добавить MessageTooLongError, CannotDeleteMessageError
│       ├── comms.repository.interface.ts ✏️ Добавить методы getConversationMessages, markMessagesAsRead
│       ├── comms.repository.prisma.ts    ✏️ Реализовать методы
│       └── comms.service.ts              ✏️ Добавить методы getConversationMessages, sendMessage, deleteMessage, markMessagesAsRead
│
├── app/
│   └── api/v1/
│       ├── conversations/
│       │   └── [conversationId]/
│       │       └── messages/             ✏️ route.ts (GET)
│       │           └── route.ts          ✏️ POST (для POST /api/v1/messages)
│       ├── messages/                     ✏️ route.ts (DELETE, PATCH)
│       │   └── [messageId]/
│       │       └── route.ts              ✏️ DELETE, PATCH
│       │
├── components/
│   └── features/
│       └── comms/
│           ├── MessageList/              🆕 Список сообщений с пагинацией
│           │   ├── MessageList.tsx
│           │   └── index.ts
│           ├── MessageItem/              🆕 Карточка одного сообщения
│           │   ├── MessageItem.tsx
│           │   └── index.ts
│           ├── MessageInput/             🆕 Поле ввода сообщения
│           │   ├── MessageInput.tsx
│           │   └── index.ts
│           └── MessageEmptyState/        🆕 Пустое состояние
│               ├── MessageEmptyState.tsx
│               └── index.ts
│
└── app/dashboard/
    └── messages/
        └── [conversationId]/             🆕 Страница диалога
            └── page.tsx
```

> **Примечание:** 🆕 — создаётся, ✏️ — изменяется

---

## 📦 Задачи по слоям

### T1: Types

| ID | Задача | Статус |
|----|--------|--------|
| T1-1 | Добавить `MessageListItem` в `comms.types.ts` | [TODO] |
| T1-2 | Добавить `ConversationMessagesResponse` в `comms.types.ts` | [TODO] |

**Целевое состояние:**
- `MessageListItem`: `{ messageId: string, content: string, createdAt: Date, readAt?: Date, authorId: string, authorName: string, authorAvatar?: string, isOwn: boolean }`
- `ConversationMessagesResponse`: `{ messages: MessageListItem[], hasMore: boolean }`

**Чек-лист:**
- [ ] Добавить интерфейсы в `src/domains/comms/comms.types.ts`
- [ ] JSDoc аннотации для каждого поля
- [ ] Экспортировать типы в `src/domains/comms/index.ts`

---

### T2: Validators

| ID | Задача | Статус |
|----|--------|--------|
| T2-1 | Добавить `getMessageListQuerySchema` в `comms.validators.ts` | [TODO] |
| T2-2 | Добавить `sendMessageSchema` в `comms.validators.ts` | [TODO] |

**Целевое состояние:**
- `getMessageListQuerySchema`: валидация query-параметров (`cursor?: string`, `limit?: number`)
- `sendMessageSchema`: валидация тела сообщения (`content: z.string().min(1).max(4000)`)

**Чек-лист:**
- [ ] Добавить Zod-схемы в `src/domains/comms/comms.validators.ts`
- [ ] JSDoc аннотации с описанием валидации
- [ ] Сообщения об ошибках на русском языке

---

### T3: Errors

| ID | Задача | Статус |
|----|--------|--------|
| T3-1 | Добавить `MessageTooLongError` в `comms.errors.ts` | [TODO] |
| T3-2 | Добавить `CannotDeleteMessageError` в `comms.errors.ts` | [TODO] |

**Целевое состояние:**
- `MessageTooLongError`: наследуется от `ValidationError`, код `MESSAGE_TOO_LONG`
- `CannotDeleteMessageError`: наследуется от `ForbiddenError`, код `CANNOT_DELETE_MESSAGE`

**Чек-лист:**
- [ ] Добавить классы ошибок в `src/domains/comms/comms.errors.ts`
- [ ] JSDoc аннотации
- [ ] Экспортировать ошибки в `src/domains/comms/index.ts`

---

### T4: Repository Interface

| ID | Задача | Статус |
|----|--------|--------|
| T4-1 | Добавить `getConversationMessages` в интерфейс | [TODO] |
| T4-2 | Добавить `markMessagesAsRead` в интерфейс | [TODO] |

**Целевое состояние:**
- `ICommsRepository.getConversationMessages(conversationId: string, cursor?: string, limit?: number): Promise<MessageListItem[]>`
- `ICommsRepository.markMessagesAsRead(conversationId: string, userId: string): Promise<void>`

**Чек-лист:**
- [ ] Добавить методы в `src/domains/comms/comms.repository.interface.ts`
- [ ] JSDoc аннотации для методов
- [ ] Проверить совместимость с существующим интерфейсом

---

### T5: Repository Implementation

| ID | Задача | Статус |
|----|--------|--------|
| T5-1 | Реализовать `getConversationMessages` в Prisma | [TODO] |
| T5-2 | Реализовать `markMessagesAsRead` в Prisma | [TODO] |

**Целевое состояние:**
- `getConversationMessages`: JOIN `messages` + `users` (author) + `conversation_participants`, cursor-based пагинация
- `markMessagesAsRead`: UPDATE `messages` SET `readAt = now()` WHERE `conversationId = ? AND authorId != ?`

**Чек-лист:**
- [ ] Реализовать методы в `src/domains/comms/comms.repository.prisma.ts`
- [ ] Использовать transactions для атомарности
- [ ] Обработка ошибок через доменные ошибки
- [ ] Cursor-based пагинация через `createdAt` + `messageId`

---

### T6: Service

| ID | Задача | Статус |
|----|--------|--------|
| T6-1 | Добавить `getConversationMessages` в `CommsService` | [TODO] |
| T6-2 | Добавить `sendMessage` в `CommsService` | [TODO] |
| T6-3 | Добавить `deleteMessage` в `CommsService` | [TODO] |
| T6-4 | Добавить `markMessagesAsRead` в `CommsService` | [TODO] |

**Целевое состояние:**
- `getConversationMessages(userId, conversationId, cursor?, limit?)`: валидация доступа, вызов repository, возврат `ConversationMessagesResponse`
- `sendMessage(senderId, conversationId, content)`: валидация через `sendMessageSchema`, создание сообщения
- `deleteMessage(messageId, userId)`: проверка авторства и времени (< 5 мин), вызов repository
- `markMessagesAsRead(conversationId, userId)`: вызов repository

**Чек-лист:**
- [ ] Добавить методы в `src/domains/comms/comms.service.ts`
- [ ] Валидация через Zod-схемы
- [ ] Проверка прав доступа (BR-01, BR-02)
- [ ] Бизнес-правила (BR-03, BR-04, BR-05)
- [ ] JSDoc аннотации со спецификацией

---

### T7: DI

| ID | Задача | Статус |
|----|--------|--------|
| T7-1 | Проверить регистрацию `CommsService` в контейнере | [TODO] |

**Целевое состояние:**
- `createCommsService()` уже зарегистрирована (US-21-01, US-21-03)

**Чек-лист:**
- [ ] Проверить `src/di/container.ts`
- [ ] При необходимости добавить фабрику (если не создана)

---

### T8: API

| ID | Задача | Статус |
|----|--------|--------|
| T8-1 | Создать `GET /api/v1/conversations/[conversationId]/messages/route.ts` | [TODO] |
| T8-2 | Создать `POST /api/v1/messages/route.ts` | [TODO] |
| T8-3 | Создать `DELETE /api/v1/messages/[messageId]/route.ts` | [TODO] |
| T8-4 | Создать `PATCH /api/v1/messages/read/route.ts` | [TODO] |

**Целевое состояние:**
- `GET /api/v1/conversations/[conversationId]/messages`: история сообщений с пагинацией
- `POST /api/v1/messages`: отправка нового сообщения
- `DELETE /api/v1/messages/[messageId]`: удаление сообщения (своё, < 5 мин)
- `PATCH /api/v1/messages/read`: помечает сообщения как прочитанные

**Чек-лист:**
- [ ] Все endpoints требуют авторизации (`auth()`)
- [ ] Валидация тела через Zod-схемы
- [ ] Обработка ошибок через `instanceof BaseError`
- [ ] Возврат стандартизированного ответа `{ success: true/false, data?, error? }`
- [ ] Соответствующие HTTP-статусы (200, 201, 400, 401, 403, 404, 409, 500)

---

### T9: UI - Страницы

| ID | Задача | Статус |
|----|--------|--------|
| T9-1 | Создать `ConversationPage` (`/dashboard/messages/[conversationId]/page.tsx`) | [TODO] |

**Целевое состояние:**
- Страница с историей сообщений и полем ввода
- Проверка авторизации через `useSession()`
- Редирект на `/login` при отсутствии сессии
- Загрузка истории сообщений через `apiClient`
- Автопрокрутка к последнему сообщению
- Обработаны состояния: loading, error, empty

**Чек-лист:**
- [x] Добавить JSDoc `@page`, `@auth`, `@role`, `@spec`, `@data-flow`
- [x] Проверка авторизации через `useSession()`
- [x] Редирект на `/login` при отсутствии сессии
- [x] Загрузка истории через `apiClient`
- [x] Форма отправки сообщения
- [x] Кнопка удаления сообщения (через `onDelete` callback)

---

### T10: UI - Компоненты

| ID | Задача | Статус |
|----|--------|--------|
| T10-1 | Создать `MessageList` | [DONE] — уже существует |
| T10-2 | Создать `MessageItem` | [DONE] — уже существует |
| T10-3 | Создать `MessageInput` | [DONE] — уже существует |

**Примечание:** Все компоненты были созданы в рамках US-21-03 (отправка личных сообщений). В рамках US-21-03-02 добавлен `onDelete` callback:
- `MessageItem`: добавлен `onDelete` prop, `onClick` на кнопку удаления
- `MessageList`: добавлен `onDelete` prop, передача в MessageItem
- `ConversationPage`: добавлен `handleDeleteMessage` callback, передача в MessageList

---

## 📊 Матрица соответствия AC → Задачи

| AC | Задачи | Статус |
|----|--------|--------|
| AC-3.1: Загрузка истории | T8-1, T9-1 | ✅ |
| AC-3.2: Структура сообщения | T10-2 | ✅ |
| AC-3.3: Сортировка | T5-1, T6-1 | ✅ |
| AC-3.4: Скроллинг | T10-1 | ✅ |
| AC-3.5: Автопрокрутка | T9-1 | ✅ |
| AC-3.6: Поле ввода | T10-3 | ✅ |
| AC-3.7: Отправка | T8-2, T9-1 | ✅ |
| AC-3.8: Ограничения | T2-2, T6-2 | ✅ |
| AC-3.9: Удаление | T8-3, T10-2 | ✅ |
| AC-3.10: ESC-отмена | T10-3 | ✅ |

---

## ✅ Чек-лист валидации

### Код и типы
- [x] `npm run type-check` — 0 ошибок (только pre-existing ThemeSelector)
- [x] JSDoc аннотации на всех новых/изменённых файлах

### Целостность
- [x] Все слои связаны (Page → API → Service → Repository → DB)
- [x] Используются `apiClient` вместо `fetch()`
- [x] Callback pattern для удаления: MessageItem → MessageList → ConversationPage

### Тестирование
- [x] Backend-тесты существуют для service/repository
- [ ] E2E-тесты для страницы (опционально)

---

## 📝 История изменений

| Дата | Автор | Изменение |
|------|-------|-----------|
| 2026-07-14 | ИИ-архитектор | Создание плана |
| 2026-07-14 | ИИ-разработчик | Реализация: добавлен onDelete callback chain (MessageItem → MessageList → ConversationPage) |